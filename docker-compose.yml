version: '3.8'

# This file is deprecated. Auth DB is now managed by the platform consolidate DB (platform-db).
# See: ../docker-compose.yml

networks:
  auth-network:
    driver: bridge

volumes:
  # Legacy volume, kept for safety but not used
  auth-db-data:
    driver: local
